{
    "version" :"0.2.0",
    "configurations": [
        {
            "name": "debug makefile project",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/tensio",
            "args" : [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "setupCommands": [{
                "description": "Enable pretty-printing for gdb",
                "text": "-enable-pretty-printing",
                "ignoreFailures": true }],
            "preLaunchTask": "Build with Make"
        }
    ],
}